# Тестовое задание для AmazingHiring

### Main Setup

``` bash
# install dependencies
yarn
```

> A React.js project

``` bash
# serve at localhost:3000
yarn start

# build for production with minification
yarn build
```
